动态代理：可以在程序的执行过程中创建代理对象
通过代理对象执行方法，给目标类方法增加额外功能（功能增强）

jdk动态代理步骤：
    1。创建目标类：SomeServiceImpl目标类，给它的doSome和doOther方法增加输出时间，提交事务
    2.创建InvocationHandler接口实现类，在这个类实现给目标方法增加功能
    3.创建使用jdk中的类proxy类，创建对象。实现创建对象的功能